'use client';
import styled from 'styled-components';
import { COLORS } from '../constants';

export const FooterText = styled.p`
font-size: 1rem;
color: ${COLORS.black};
letter-spacing: 2;
text-align: center;
padding: 16px;
margin: 24px 0;
`;